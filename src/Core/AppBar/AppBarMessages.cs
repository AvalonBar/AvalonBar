using System;
using System.Collections.Generic;
using System.Text;

namespace Sidebar.Core
{
    internal enum AppBarMessages
    {
        NewAppBar = 0,
        Remove = 1,
        QueryPos = 2,
        SetPos = 3,
        GetState = 4,
        GetTaskBarPos = 5,
        Activate = 6,
        GetAutoHideBar = 7,
        SetAutoHideBar = 8,
        WindowPosChanged = 9,
        SetState = 10
    };
}
